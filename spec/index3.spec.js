const { expect } = require('chai');
const overlapFinder = require('../index3');

describe('OverlapFinder()', () => {
  it('should be a function', () => {
    expect(overlapFinder).to.be.a('function');
  })
})
